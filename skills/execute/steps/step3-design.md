# Step 3: 기술 설계

`{prd_path}/features/[기능명]/context.md`의 수집 컨텍스트를 읽은 후,
`shared/design.md`를 Read하고 그 지시를 따르세요.

> `{prd_path}` = `.opensmith/config.json`의 `prd_path`. 미설정 시 `.opensmith/docs/prd`.

설계 결과는 `.opensmith/docs/design/[기능명].md`에 저장합니다.

완료 후 다음 스텝: `execute/steps/step4-approve.md`
